apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metasploit.fullname" . }}-test-connection"
  labels:
    {{- include "metasploit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metasploit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
