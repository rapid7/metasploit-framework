# @note 'Payloads' is pluralized, much like 'Modules' in `Msf::Modules` to prevent a constant name collision and the
#   need to fully-qualify constants in older module files.
#
# Namespace for all ruby `Class` which are payloads.
module Msf::Payloads

end